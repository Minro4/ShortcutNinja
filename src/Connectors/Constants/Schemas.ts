import { CONFIG_PATH } from "./general";
import * as path from "path";

export const SCHEMAS_PATH = path.join(CONFIG_PATH, "Schemas");
export const UNPROCESSED_SCHEMAS_PATH = path.join(CONFIG_PATH, "SchemasUnprocessed");