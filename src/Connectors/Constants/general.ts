import * as path from "path";

export const APP_NAME = "keymap-manager";
export const CONFIG_PATH = "src/Connectors/Config";
export const DEFAULT_MAPPINGS_PATH = path.join(CONFIG_PATH, "defaultMappings");
export const IDE_MAPPINGS_PATH = path.join(CONFIG_PATH, "ideMappings");
